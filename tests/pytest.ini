[pytest]
testpaths = unit integration
markers =
    integration: needs the live stack (docker compose + stub OWUI)
    slow: takes tens of seconds (idle sweeps, cache grace windows)
addopts = -ra --strict-markers --timeout=300
